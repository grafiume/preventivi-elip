// config.js (template) — Place in the ROOT of preventivi-elip (same folder as index.html)
window.preventiviConfig = {
  // REQUIRED — fill with your Supabase project URL and anon key for the *preventivi* database
  url:   "https://YOUR-PREVENTIVI-PROJECT.supabase.co",
  anon:  "YOUR-ANON-KEY",
  // (optional) schema/table names if your app reads them
  schema: "public",
  preventiviTable: "preventivi"
};
